<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/nav_communicate"
            android:icon="@drawable/ic_chat_bubble"
            android:title="@string/menu_communicate" />
        <item
            android:id="@+id/nav_live_captioning"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="@string/captions_title" />
        <item
            android:id="@+id/nav_dictionary"
            android:icon="@drawable/ic_book"
            android:title="@string/visual_dictionary_title" />
        <item
            android:id="@+id/nav_contacts"
            android:icon="@drawable/ic_people"
            android:title="@string/menu_contacts" />
    </group>

    <item android:title="@string/menu_others">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings"
                android:title="@string/menu_settings" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_exit_to_app"
                android:title="@string/menu_logout" />
        </menu>
    </item>
</menu>
